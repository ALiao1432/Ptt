<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewArticle"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <TextView
        android:id="@+id/articleInfoText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|center_vertical"
        android:paddingStart="@dimen/layout_margin"
        android:paddingEnd="@dimen/layout_margin"
        android:text="@string/unselected_article_hint"
        android:textAlignment="center"
        android:textColor="@color/themeDarkSecondaryText" />

    <include
        android:id="@+id/pollOptionBottomSheet"
        layout="@layout/layout_bottom_sheet_poll_option" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>